{
  "name": "backend-for-developers-chat-app",
  "version": "1.0.0",
  "description": "complete api for developers chat app from creating account, logging in, uploading and sending of images, gifs and sending emojis",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js"
  },
  "author": "olingo maxwell",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "express": "^4.18.1",
    "mongoose": "^6.4.1"
  },
  "devDependencies": {
    "dotenv": "^16.0.1",
    "nodemon": "^2.0.18"
  }
}
